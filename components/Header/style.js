import {StyleSheet}from 'react-native'

const estilo = StyleSheet.create({

    titulo: {
        fontSize: 35,
        fontWeight: 'bold',
        marginTop: 20,
      },
      subtitulo: {
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 15,
      },
      tituloCard: {
        fontSize: 15,
        fontWeight: 'bold',
        color: '#1d3557',
      },


})

export default estilo;